import { AdminuserDirective } from './adminuser.directive';

describe('AdminuserDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminuserDirective();
    expect(directive).toBeTruthy();
  });
});
